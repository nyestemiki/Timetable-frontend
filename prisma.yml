endpoint: https://eu1.prisma.sh/nyeste-miklos/timetable/dev
datamodel: datamodel.prisma
# secret: ${env:PRISMA_SECRET}
generate:
    - generator: graphql-schema
      output: ./src/generated/schema.graphql
hooks: 
  post-deploy:
    - graphql get-schema -p prisma
    - prisma generate